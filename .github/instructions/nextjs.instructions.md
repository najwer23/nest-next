# Next.js Standards

- Use Server Components by default.
- Add "use client" only when required.
- Client components must handle:
  - pending state
  - failed state
  - successful state

Avoid unnecessary manual loading flags.